# 支持与验收矩阵

目标平台只有 **Minecraft 1.20.1 / Forge 47.3.0 / Java 17**。下面区分实现、运行证据和仍需完成的验收，不能把编译通过当作兼容性结论。

## 当前边界

| 领域 | 当前实现 | 证据与边界 |
| --- | --- | --- |
| 公共菜单、HUD、设置、主题、世界与会话 API | 统一注册、排序、冻结；内置功能使用同一入口 | 独立附属项目仅依赖发行 JAR 编译；其 JAR 已在隔离 Forge 客户端加载，菜单/HUD/主题/世界标记可见；跨维度与重连留待框架验收 |
| 原生字段、滚动区、卡片与确认弹层 | 保留 Minecraft 输入控件及 Forge GUI 层 | 已验证 Unicode/O 输入、嵌套 Tab 自动滚动、弹层取消/确认、缩放及同实例重开；预填内容另有实际绘制检查。未声称操作系统输入法组合过程已验证 |
| O 菜单与装备层级 | 已有生产入口，动态列表按 ID 保持选择 | 原生指针打开独立附属表单；13 面板/40 行夹具能访问末项，重排后仍激活原条目；侧卡越界填充被裁剪 |
| 模组设置 | P5 独立风格、动态分类与自有持久化回调 | 13 分类可达末项；字段失焦后不接收输入，保存不串组；超过单行容量的主题可循环选到末项 |
| 玩家属性页 | 独立于原版累计统计；真实菜单入口、公共卡片和有界滚动区 | 已沿生产入口打开；长表可滚动到最后一项，完成按钮保留在视口内，服务器同步的属性值在同一页面更新 |
| 原生统计与进度 | 删除旧的“已完成列表”，直接使用原生统计和完整进度图控制器 | 菜单实际指针路由已打开两页；收到服务器累计统计，原生完成按钮返回菜单 |
| 框架统一恢复 | 持久化主开关、F8、标题/暂停入口、启动强制安全模式 | 集成世界验证原生背包持物/槽位连续性及服务器转移；所有 HUD/世界贡献受控；前台操作系统 F8 已实际切换；安全模式贡献调用数为零 |
| 原生背包与储物容器 | 生存背包/箱子/潜影盒/漏斗/发射器/投掷器换肤；创造背包保留原生内部绘制并装饰边框 | 实际原生槽位转移、2×2 合成、配方书、创造搜索过滤及服务端创造物品插入；真实操作系统 Shift-click 转移八个苹果且光标为空 |
| 合成与工作站 | 合成台、熔炉/高炉/烟熏炉换肤；其余复杂工作站装饰，不重造控制器 | 集成服务端确认合成、完整熔炼/酿造、铁砧命名、下界合金升级、附魔/祛魔、切石、红色旗帜图案、地图扩展、信标速度主效果与村民交易；原生进度/配方/三维模型可见 |
| 服务端物品逻辑 | 服务端校验、守恒与冷却检查 | 已在集成服务端真实玩家上运行；尚未完成独立客户端发包验收 |
| 专用服务端 | 公共包与客户端入口已分离 | 仍需实际启动验收；不得自动接受 EULA |
| Fabric | 无发布模块 | 不支持，不生成 Fabric 产物 |
| 未知模组界面、光影与其他 HUD 模组 | 不推定兼容 | 需要明确适配及实际组合验证 |

## 原版域实施范围

以下范围分域推进；恢复基础、背包/储物容器和工作站已在真实原生界面验证，前端/文字域仍待注册适配器后验收：

- HUD、主菜单入口、暂停、生存/创造背包及真实容器贯通。
- 箱子、潜影盒、漏斗、发射器等储物容器，以及合成、熔炉族、酿造、铁砧、锻造、附魔、砂轮、切石、织布、制图、信标、交易等工作站。
- 聊天、书籍、告示牌、统计、进度及相关文字/信息界面；保留原有输入、服务器交互与确认语义。
- 标题、世界/服务器选择、选项及游戏前端；加载、连接、断开、死亡与转场。
- 界面策略和恢复入口已实现；精确类匹配，未知类默认保留，不对所有 `Screen` 或所有纹理做无差别替换。

每个领域须按其真实入口和状态验证。仅打开一个白色样式面板、不使用原生容器控制器，或用本地物品数组代替服务器菜单，都不算该领域完成。

## 验收方法

1. 普通测试覆盖注册边界、状态转换、配置迁移等确定性契约。
2. 独立附属项目只以产出的发行 JAR 为依赖，检查其导入不越过公共 API。
3. 隔离客户端验证真实界面、输入、滚动、焦点、弹层、保存与世界生命周期，并保留截图和日志。
4. 专用服务端和至少两个独立客户端验证网络路径；集成世界不能替代这一步。
5. 发行 JAR 检查不包含开发预览、测试夹具或独立示例类。

公开 API 阶段的本机证据目录：`D:/saomenu-verify/framework-api-final`；运行日志位于同名前缀的 `-client/logs`。该阶段共 218 项普通测试通过；发行 JAR 含 54 个 API 类且不含开发预览或示例类。示例独立配置已保存 Unicode 标签和拖动坐标。Javadoc JAR 构建通过，但仍有公开成员注释完整性警告，未将其屏蔽。

恢复基础证据：`D:/saomenu-verify/recovery` 与 `D:/saomenu-verify/recovery-safe`；日志在对应 `-client/logs`。两次模式均正常退出；强制安全模式不能通过 F8、界面或 API 重启。原生背包中持有的八个苹果跨开关保持，并通过原生鼠标槽位操作移到目标槽，服务端确认。升级光环残留已修复：同一地面区域的青色粒子像素由修复前 8199 降到 0，不清空原版粒子引擎。当前发行 JAR 有 53 个 API 类（删除重复的包内绘图实现），不含开发/示例类，也不含原版默认字体覆盖；普通测试仍为 218 项全绿。

容器域证据：`D:/saomenu-verify/containers`，日志位于 `containers-client/logs`，出现 `containers native checks passed` 并正常退出。熔炉箭头的原生进度宽度保持不变，透明底材质去掉了旧灰色底块；目标背景像素由 `(198,198,198)` 变为当前表面色 `(234,235,236)`。创造搜索通过原生字符输入真正过滤为两种橡木木板，而不是只给 `EditBox.setValue` 后检查字段。未知背包子类仍呈现原生底板，F8 中途恢复不换容器实例。本轮不宣称配方幽灵自动放置、创造快捷栏保存/加载、信标次效果或操作系统输入法组合已经验证。

预览目录不得指向用户个人实例。外部发布、推送及法律条款接受都不属于本地构建授权。
